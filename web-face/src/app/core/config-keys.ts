/** The namespaced config-entry keys currently read by the frontend — see ConfigController. */
export const CONFIG_KEYS = {
  googleSsoClientId: 'sso.google.client.id',
  googleMapsApiKey: 'map.google.api.key',
} as const;
